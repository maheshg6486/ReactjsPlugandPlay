import { Card, Typography } from "@material-ui/core";
import React from "react";

const TimeCard = (props) => {
  return (
    <Card>
      <Typography variant="h6">Time Card</Typography>
    </Card>
  );
};

export default TimeCard;
